import React from "react";
import { Container, Typography } from "@mui/material";

const PrivatePage = () => {
    return (
        <Container>
            <Typography variant="h4">Private Page</Typography>
        </Container>
    );
};

export default PrivatePage;
 